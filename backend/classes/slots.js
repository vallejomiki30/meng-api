export class Slots {
    constructor(name,parkingSize,position,location,sizePermitted){
        this.name = name
        this.parkingSize = parkingSize
        this.position = position
        this.location = location
        this.sizePermitted = sizePermitted
    }
}