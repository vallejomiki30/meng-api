export class ParkingSlot {
    constructor(location,slotName,vehicleSize,datetime) {
        this.location = location
        this.slotName = slotName
        this.vehicleSize = vehicleSize
        this.datetime = datetime
    }

}